---
title: Kiro Workbench Docs
description: Install kiroctl, bind a client-scoped project, and use the delivered Kiro capabilities — agents, skills, gates, steering, missions and QA.
template: splash
hero:
  tagline: A governed Kiro CLI setup — an Opus/Sonnet agent framework, on-demand skills, mission mode, render-backed QA, client-bound SSO, and enforcement gates. Inert after install until you arm a gate.
  actions:
    - text: Get started
      link: ./start/overview/
      icon: right-arrow
      variant: primary
    - text: Browse capabilities
      link: ./capabilities/overview/
      icon: open-book
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## What you'll find here

<CardGrid stagger>
  <Card title="Install & first run" icon="rocket">
    Clone the two repos, run the installer with `KIRO_REGISTRY_ROOT`, and launch
    a client-bound project with `kiroctl` — macOS, Linux/WSL, and Windows.
  </Card>
  <Card title="Know your capabilities" icon="puzzle">
    The `dev`/`coder`/`scout` agents, 21 skills, 9 gates, and 5 steering files —
    what each does and when it fires. Gates stay inert until you arm them.
  </Card>
  <Card title="Missions & QA" icon="approve-check">
    Drive multi-phase work autonomously with mission mode and the `/goal` loop,
    proven by a render-backed QA protocol with two-gate verdicts.
  </Card>
  <Card title="Author & customize safely" icon="pencil">
    Register a custom capability with `kiroctl onboard`, and update without losing
    your edits — bolt-on, never subtract.
  </Card>
</CardGrid>
